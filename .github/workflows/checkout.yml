name: checkout example

on:
  workflow_dispatch:

  push:
    branches:
      - main

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.1
      
      - name: verify
        run: ls -alhR
